{
    "name": "react-sub-app",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite --port 8082",
        "build": "vite build",
        "build:preview": "vite build --mode preview",
        "build:prod": "vite build --mode production",
        "preview": "npx serve dist -l 8082 -s --cors --no-request-logging",
        "preview:prod": "npx serve dist -l 8082 -s --cors",
        "deploy": "bash deploy.sh"
    },
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "react-router-dom": "^6.20.0",
        "vite-plugin-qiankun": "^1.0.15"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.2.0",
        "terser": "^5.44.1",
        "vite": "^5.0.0"
    }
}